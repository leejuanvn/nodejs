{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  // Thay đổi tên file nếu cần
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"  // Chỉ định file chính
    }
  ]
}
